import 'package:flutter/material.dart';

class ClinicReservationScreen extends StatelessWidget {
  const ClinicReservationScreen({Key? key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
